package com.zhentao.three.day01;

public class Test {
    public static void main(String[] args) {  // 程序入口函数   通过main快速生成
        System.out.println(123);
        System.out.println(123);
        System.out.print(123);
        System.out.print(123);
        System.out.println(123);
        System.out.print(123);

        System.out.println("qw\nerty\tui");

        /* println()输出后换行   print() 输出后不换行
        * 转义字符  \n 换行   \t 退到下一个水平制表符位置  可以理解为 空格*/

        /* 一个Java文件 可以写多个class类 但是只能有一个被public修饰 而且被public修饰的类名
        *  必须跟文件名保持一致   多个class类会生成多个.class文件*/
        /* Java中 声明变量 常量必须指定数据类型  语法：  数据类型 变量名/常量名 = 值
        * 而且 Java语句后面必须跟 ;
        *  关键字： 是指在程序中，Java已经定义好的单词，具有特殊含义。
        * Java关键字
            abstract	assert	boolean	break	byte
            case	catch	char	class	const
            continue	default	do	double	else
            enum	extends	final	finally	float
            for	goto	if	implements	import
            instanceof	int	interface	long	native
            new	package	private	protected	public
            return	strictfp	short	static	super
            switch	synchronized	this	throw	throws
            transient	try	void	volatile	while*/

        /* 标识符概述
                程序中的各种数据类型的对象，如符号常量、变量、方法、类等都需要一定的名称，这种名称叫做标识符。
                标识符组成规则
                1.标识符可以包含 英文字母26个(区分大小写) 、 0-9数字 、 $（美元符号） 和 _（下划线） 。
                2.首字母必须是英文字母、下划线、$。
                3.不能以数字开头，不能是Java中的关键字
                4.命名区分大小写，为了提高阅读性，需“见名知意”  一般变量使用 小驼峰  常量使用纯大写字母 多个单词用下划线拼接
                类或者接口名 大驼峰 StudentName。
          */

        // 注释  单行注释 //  多行注释 /* 内容*/  文档注释 /** 内容*/

        /* Java 的数据类型
        基本数据类型
            字节型 byte     1字节  -128—— 127
            短整型 short    2字节
            整型   int      4字节
            长整型 long     8字节   值后面需要加L
            单精度  float   4字节   值后面需要加F
            双精度  double  8字节
            布尔型  boolean 1字节
            字符型  char    2字节  必须使用单引号


        变量必须初始化（赋值）后才能使用
        引用数据类型 ：包括类、数组、接口、枚举
        字符串用 String  必须使用双引号
        字符类型 在或者比较的时候 会将该字符转换成对应的ASCII码再进行运算或者比较
        *   */
        byte b = 12;
        short s = 123;
        int i = 1234;
        long l = 123453432432432342L;
        float f = 12.3F;
        double d = 12.3;
        boolean boo = true;
        char c = 'y';

        String str = "你好 Java";
        System.out.println(b);
        System.out.println(s);
        System.out.println(i);
        System.out.println(l);
        System.out.println(f);
        System.out.println(d);
        System.out.println(boo);
        System.out.println(c);


        System.out.println(b+b);
        System.out.println(c+c);

        char m = 'M';
        char n = 'n';
        System.out.println(m>n);
        // 判断 一个字符是否是小写英文字母
       //        字符>='a'&& 字符<='z'
        System.out.println(m>='a'&&m<='z');

  /* 数组： 存储相同数据类型 长度固定的容器
        *
        * 分类  按照维度 一维数组  二维数组 多维数组
        *    按照数据类型  基本数据类型数组  引用数据类型数组
        * 定义一维数组的方式：
        *     1. 静态初始化    数据类型 [] 数组名 = {元素,元素,元素};
        *     2. 动态初始化    数据类型 [] 数组名 = new 数据类型[长度]
        *   访问和修改数组元素 通过下标  下标从0开始 最大下标是数组长度-1
        *   数组长度通过length获取
        *  数组的默认值 整型都是 0 , 浮点型都是0.0, 布尔值 false ,char是Unicode空字符 , 引用数据类型 null
        *  数组中常见的异常：  数组下标越界  空指针异常
        * */

        // 定义一个数组 [1,2,3]
//        int[] arr = {1,2,3};
//        // 使用动态初始化 创建一个长度为5 的int类型的 数组
//        String [] arr2 = new String[5];
//        System.out.println(arr.length);
//        System.out.println(arr2.length);
//        // 通过下标访问数组元素
//        System.out.println(arr[0]);
//        System.out.println(arr[1]);
//        // 修改下标为2 的元素
//        arr[2]=99;
//        System.out.println(arr[2]);
//
//        System.out.println(arr2[0]);
        /*数组常见异常*/
//        int [] arr = {1,3,5,7};
//        System.out.println(arr[3]);
////        System.out.println(arr[4]);
//
//        String [] arr2 = new String[3];
//        arr2[0]="qwrer";
//        System.out.println(arr2[0].charAt(0));
//        System.out.println(arr2[1].charAt(0));
        /*遍历数组
        *  for循环 ， foreach遍历   语法  for(数据类型 变量名:数组名){} 变量名代表每一个元素
        * foreach 遍历不会修改原数组 （基本数据类型）  for循环遍历可以修改数组元素
        * */
//         int [] arr = {1,3,5,7,9};
//        for (int i = 0; i < arr.length; i++) {
//            System.out.println(arr[i]);
//        }
//        // 反向遍历
//        for (int i = arr.length-1; i >0 ; i--) {
//            System.out.println(arr[i]);
//        }
//
//        for(int item:arr){
//            System.out.println(item);
//        }

//                for (int i = 0; i < arr.length; i++) {
//                    arr[i]=99;
//                }

//                        for(int item:arr){ // 无法修改数组元素
//                            item=99;
//                        }

//                for (int i = 0; i < arr.length; i++) {
//                    System.out.println(arr[i]);
//                }


        /* 基本数据类型 和引用数据类型 存储区别
        *  基本数据类型存储再栈中   引用数据类型的值存储再堆 同样会再栈中开辟一个空间 存储引用数据类型的变量
        *   但是这个变量存储的是该引用数据类中再堆中的地址    引用数据类型 无论是比较 还是赋值 都是对地址进行操作
        *   如果两个变量指向相同的地址 那么其中一个变量修改后另外一个变量也随着修改
        *  */

        int [] arr = {1,2,3};
        int [] arr2 = {1,2,3};
        int [] arr3 = arr;  // arr 和arr3指向同一个地址
        System.out.println(arr==arr2);
        System.out.println(arr==arr3);

        arr3[1] = 100;
        System.out.println(arr3[1]);  // 100
        System.out.println(arr2[1]);  //2
        System.out.println(arr[1]);   //100

        /* Arrays 工具类
        *  toString() 将数组转换成字符串形式 返回值是字符串 语法 Arrays.toString(数组名);
        *  需要接收返回值 然后打印
        * */
        System.out.println(arr);
        System.out.println(Arrays.toString(arr));
  /* 冒泡排序 */
        // 从小到大    双重for循环  内外各减-1  内层减外层 变量相交换
//        int [] arr = {11,34,12,7,32,23,67,45};
//        for (int i = 0; i < arr.length-1; i++) {
//            for (int j = 0; j < arr.length-1-i; j++) {
//                if(arr[j]<arr[j+1]){
//                    // 定义一个变量存储其中一个    存储哪个 先给谁赋值
//                    int temp =  arr[j];
//                    arr[j]=arr[j+1];
//                    arr[j+1]=temp;
//                }
//            }
//        }
//
//        Arrays.sort(arr);  // 只能升序
//        System.out.println(Arrays.toString(arr));



        /* 删除数组元素后 保证数组的连贯性 */
//        int [] arr = {11,34,12,7,7,7,67,45};  // 把7删掉  [11,34,12,32,23,67,45,0]
//        // 先确定7的位置
//        for (int i = 0; i < arr.length; i++) {
//            if(arr[i]==7){
//                // 从当前位置 开始 当前元素等于后一位元素
//                for (int j = i; j < arr.length-1; j++) {
//                    arr[j]=arr[j+1];
//                }
//                // 将最后一位赋值为 默认值
//                arr[arr.length-1]=0;
//                // 删除之后后面元素补上来后重新判断当前位置元素是否符合条件
//                i--;
//            }
//        }
//
//        System.out.println(Arrays.toString(arr));

        /* 二维数组 ： 以一维数组作为数组元素的数组    （数组的元素还是数组）*/
        //   定义二维数组的语法
         // 静态初始化  语法：  数据类型 [][] 数组名 = {{元素,元素},{元素,元素}...}
        // 访问或者赋值 二维数组元素 需要使用两个下标   数组名[i][j]
        // 动态初始化   数据类型 [][] 数组名 = new 数据类型[len1][len2];
        //   len1代表数组中几个一维数组 len2 代表每个一位数组长度


//        int [] [] arr = {{1,3,5},{2,4,6}};
//        System.out.println(Arrays.deepToString(arr));
//
//        // 动态初始化
//        int[][] arr2 =new int[2][3];

        // 初始化后 给二维数组赋值

        /* 通过两个下标赋值*/
//        arr2[0][0]=1;
//        arr2[0][1]=3;
//        arr2[0][2]=5;
//        arr2[1][0]=2;
//        arr2[1][1]=4;
//        arr2[1][2]=6;
        /*通过一维数组赋值*/
//        int [] minArr = {1,3,5};
//        int [] minArr2 = {2,4,6};
//        arr2[0]=minArr;
//        arr2[1]=minArr2;
//
//        System.out.println(Arrays.deepToString(arr2));
//        System.out.println(Arrays.toString(arr2[0]));
//        System.out.println(Arrays.toString(arr2[1]));


        /* 遍历二维数组 */
        int [] [] arr = {{1,3,5},{2,4,6}};
        for (int i = 0; i < arr.length; i++) {
            for (int j = 0; j <arr[i].length ; j++) {
                System.out.println(arr[i][j]);
            }
        }

    }
}
