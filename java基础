package com.zhentao.three.day01;

public class Test {
    public static void main(String[] args) {  // 程序入口函数   通过main快速生成
        System.out.println(123);
        System.out.println(123);
        System.out.print(123);
        System.out.print(123);
        System.out.println(123);
        System.out.print(123);

        System.out.println("qw\nerty\tui");

        /* println()输出后换行   print() 输出后不换行
        * 转义字符  \n 换行   \t 退到下一个水平制表符位置  可以理解为 空格*/

        /* 一个Java文件 可以写多个class类 但是只能有一个被public修饰 而且被public修饰的类名
        *  必须跟文件名保持一致   多个class类会生成多个.class文件*/
        /* Java中 声明变量 常量必须指定数据类型  语法：  数据类型 变量名/常量名 = 值
        * 而且 Java语句后面必须跟 ;
        *  关键字： 是指在程序中，Java已经定义好的单词，具有特殊含义。
        * Java关键字
            abstract	assert	boolean	break	byte
            case	catch	char	class	const
            continue	default	do	double	else
            enum	extends	final	finally	float
            for	goto	if	implements	import
            instanceof	int	interface	long	native
            new	package	private	protected	public
            return	strictfp	short	static	super
            switch	synchronized	this	throw	throws
            transient	try	void	volatile	while*/

        /* 标识符概述
                程序中的各种数据类型的对象，如符号常量、变量、方法、类等都需要一定的名称，这种名称叫做标识符。
                标识符组成规则
                1.标识符可以包含 英文字母26个(区分大小写) 、 0-9数字 、 $（美元符号） 和 _（下划线） 。
                2.首字母必须是英文字母、下划线、$。
                3.不能以数字开头，不能是Java中的关键字
                4.命名区分大小写，为了提高阅读性，需“见名知意”  一般变量使用 小驼峰  常量使用纯大写字母 多个单词用下划线拼接
                类或者接口名 大驼峰 StudentName。
          */

        // 注释  单行注释 //  多行注释 /* 内容*/  文档注释 /** 内容*/

        /* Java 的数据类型
        基本数据类型
            字节型 byte     1字节  -128—— 127
            短整型 short    2字节
            整型   int      4字节
            长整型 long     8字节   值后面需要加L
            单精度  float   4字节   值后面需要加F
            双精度  double  8字节
            布尔型  boolean 1字节
            字符型  char    2字节  必须使用单引号


        变量必须初始化（赋值）后才能使用
        引用数据类型 ：包括类、数组、接口、枚举
        字符串用 String  必须使用双引号
        字符类型 在或者比较的时候 会将该字符转换成对应的ASCII码再进行运算或者比较
        *   */
        byte b = 12;
        short s = 123;
        int i = 1234;
        long l = 123453432432432342L;
        float f = 12.3F;
        double d = 12.3;
        boolean boo = true;
        char c = 'y';

        String str = "你好 Java";
        System.out.println(b);
        System.out.println(s);
        System.out.println(i);
        System.out.println(l);
        System.out.println(f);
        System.out.println(d);
        System.out.println(boo);
        System.out.println(c);


        System.out.println(b+b);
        System.out.println(c+c);

        char m = 'M';
        char n = 'n';
        System.out.println(m>n);
        // 判断 一个字符是否是小写英文字母
       //        字符>='a'&& 字符<='z'
        System.out.println(m>='a'&&m<='z');

    }
}
